{{!< default}}
              
    <header class="big {{#if @blog.cover}}cover" style="background-image: url({{@blog.cover}})"{{else}}no-cover"{{/if}}>
        <h1 class="home-head">
            <img src="{{asset "img/design/script.png"}}" alt="Logo"> {{@blog.title}}</a>
        </h1>
        {{#is "home"}}
        <div class="meta">
            <span class="small">Un simple carnet de carnets entre amis</span> <a class="info" href="/a-propos" style="margin-left:1em"><span class="oi" data-glyph="info"></span></a>
        </div>
        {{/is}}
    </header>

    <div id="wrapper">
    {{! The tag below includes the post loop - partials/loop.hbs }}
    {{> "loop"}}
